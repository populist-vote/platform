[workspace]
default-members = ["server"]
members = [
    "auth",
    "config",
    "db",
    "embed_validation",
    "graphql",
    "mailers",
    "scripts",
    "scrapers",
    "server",
    "open-fec",
]
exclude = ["geocodio", "legiscan", "open-secrets", "votesmart"]
resolver = "2"
